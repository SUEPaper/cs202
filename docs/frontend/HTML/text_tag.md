---
id: text-tag
sidebar_position: 3
---

# 文本标签

## 标题标签（h1~h6）
大部分结构化文本由标题和段落组成。不管是小说、报刊、教科书还是杂志等。

内容结构化会使读者的阅读体验更轻松，更愉快。

在 HTML 中，每个标题（Heading）都必须被包裹在一个标题元素中：
```bash
<h1>我是文章的标题</h1>
```
一共有六种标题元素标签——h1、h2、h3、h4、h5 和 h6。每个元素代表文档中不同级别的内容：&#60h1> 表示主标题，&#60h2> 表示二级子标题，&#60h3> 表示三级子标题，依此类推。
```bash
<h1>我是文章的主标题</h1>
<h2>我是文章二级子标题</h2>
<h3>我是文章三级子标题</h3>
<h4>我是文章四级子标题</h4>
<h5>我是文章五级子标题</h5>
<h6>我是文章六级子标题</h6>
```
所涉及的元素具体代表什么，完全取决于作者编辑的内容，只要层次结构是合理的。在创建此类结构时，只需要记住一些最佳实践：

最好只对每个页面使用一次 &#60h1>——这是顶级标题，所有其他标题位于层次结构中的下方。

请确保在层次结构中以正确的顺序使用标题。不要使用 &#60h3> 来表示副标题，后面再跟 &#60h2> 来表示二级副标题——这是没有意义的，会导致奇怪的结果。

在现有的六个标题层次中，除非觉得有必要，否则应该争取每页使用不超过三个。有很多层次的文件（例如，深层次的标题层次）会变得笨重，难以浏览。在这种情况下，如果可能的话，建议将内容分散到多个页面。
## 段落标签（p）
在 HTML 中，每个段落是通过 &#60p> 元素标签进行定义的，比如下面这样：
```bash
<p>我是一个段落，千真万确。</p>
```
这里举一个例子。在一个故事中，&#60h1> 表示故事的名字，&#60h2> 表示每个章节的标题，&#60h3> 表示每个章节下的子标题，以此类推。
```bash
<h1>三国演义</h1>

<p>罗贯中</p>

<h2>第一回 宴桃园豪杰三结义 斩黄巾英雄首立功</h2>

<p>话说天下大势，分久必合，合久必分。周末七国分争，并入于秦。及秦灭之后，楚、汉分争，又并入于汉……</p>

<h2>第二回 张翼德怒鞭督邮 何国舅谋诛宦竖</h2>

<p>且说董卓字仲颖，陇西临洮人也，官拜河东太守，自来骄傲。当日怠慢了玄德，张飞性发，便欲杀之……</p>

<h3>却说张飞</h3>

<p>却说张飞饮了数杯闷酒，乘马从馆驿前过，见五六十个老人，皆在门前痛哭。飞问其故，众老人答曰：“督邮逼勒县吏，欲害刘公；我等皆来苦告，不得放入，反遭把门人赶打！”……</p>
```
## 换行符（br）
在HTML中，用于创建替换的元素是&#60br>标签。&#60br>标签是一个空间标签，它没有屏蔽标签，因此不需要在末尾添加斜杠。

要在HTML文档中插入一个换行符，只需要在所需要的位置插入&#60br>标签即可。例如：
```bash
<p>这是一行文本。<br>这是另一行文本。</p>
```
在上面的示例中，&#60br>标签插入在第一行的末尾，用于创建两行文本。

需要注意的是，HTML是一种标记语言，它的主要目的是为文档提供结构和语言，而不是用于布局。因此，如果您希望在页面上进行布局，请考虑使用CSS来控制元素的样式和布局，而不是仅仅依靠&#60br>标签来创建替换行。
## 加粗（b）、斜体（i）等文本样式
在HTML中，可以使用一些标签来设置文本的样式，包括加粗、斜体等。
1、加粗文本：使用&#60b>标签或&#60strong>标签将文本包围起来。
```bash
<b>这是加粗文本</b>
<strong>这也是加粗文本</strong>
```
2、斜体文本：使用&#60i>标签或&#60em>标签将文本包围起。
```bash
<i>这是斜体文本</i>
<em>这也是斜体文本</em>
```
3、下划线文本：使用&#60u>标签将文本包围起。
```bash
<u>这是下划线文本</u>
```
4、上标和下标：使用&#60u>标签将文本包围起。
```bash
<p>
  咖啡因的化学方程式是
  C<sub>8</sub>H<sub>10</sub>N<sub>4</sub>O<sub>2</sub>。
</p>
<p>如果 x<sup>2</sup> 的值为 9，那么 x 的值必为 3 或 -3。</p>
```
5、剪除线文本：当你使用日期、化学方程式、和数学方程式时会偶尔使用上标和下标，以确保它们的正确含义。&#60sup> 和 &#60sub> 元素可以解决这样的问题。
```bash
<s>这是剪除线文本</s>
<del>这也是剪除线文本</del>
```
剪除线文本的显示样式可以通过CSS进行自定义。可以使用CSS的text-decoration属性来控制剪除线的颜色、粗细等样式。
```bash
<style>
  .strikethrough {
    text-decoration: line-through;
    color: red;
  }
</style>

<span class="strikethrough">这是自定义样式的剪除线文本</span>
```
在上面描述的例子中，我们确定了一个.strikethrough类型，它应用了line-through的text-decoration属性和红色的color属性，从而创建了自定义样式的剪除线文本。然后，&#60span>标签使用了这种类型来应用样式。

###展示计算机代码
有大量的 HTML 元素可以来标记计算机代码：
- &#60code>：用于标记计算机通用代码。
- &#60pre>：用于保留空白字符（通常用于代码块）——如果文本中使用了缩进或多余的空白，浏览器将忽略它，你将不会在呈现的页面上看到它。但是，如果你将文本包含在 - &#60pre>&#60/pre> 标签中，那么空白将会以与你在文本编辑器中看到的相同的方式渲染出来。
- &#60var>：用于标记具体变量名。
- &#60kbd>：用于标记输入电脑的键盘（或其他类型）输入。
- &#60samp>：用于标记计算机程序的输出。
```bash
<pre><code>const para = document.querySelector('p');

para.onclick = function() {
  alert('噢，噢，噢，别点我了。');
}</code></pre>

<p>
  请不要使用 <code>&lt;font&gt;</code> 、 <code>&lt;center&gt;</code> 等表象元素。
</p>

<p>
  在上述的 JavaScript 示例中，<var>para</var> 表示一个段落元素。
</p>

<p>按 <kbd>Ctrl</kbd>/<kbd>Cmd</kbd> + <kbd>A</kbd> 选择全部内容。</p>

<pre>$ <kbd>ping mozilla.org</kbd>
<samp>PING mozilla.org (63.245.215.20): 56 data bytes
64 bytes from 63.245.215.20: icmp_seq=0 ttl=40 time=158.233 ms</samp></pre>
```